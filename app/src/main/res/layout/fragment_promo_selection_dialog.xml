<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_bottom_sheet"
    android:padding="20dp">

    <!-- Индикатор перетаскивания (Handle) -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="16dp"
        android:background="#D1D1D6" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Доступные акции"
        android:textColor="#1B1B1F"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:text="Выберите одну или несколько акций для применения"
        android:textColor="#757575"
        android:textSize="14sp" />

    <!-- Список доступных акций -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvPromos"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:minHeight="100dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginTop="20dp">

        <!-- ДОБАВЛЕНО: Кнопка подтверждения выбора (ID: btnConfirmPromo) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnConfirmPromo"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginBottom="12dp"
            android:text="Применить выбранные акции"
            android:textColor="@android:color/white"
            app:backgroundTint="#10b981"
            app:cornerRadius="12dp" />

        <!-- Кнопка Пропустить (без акции) -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSkipPromo"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:text="Сохранить без акции"
            android:textColor="#757575"
            app:strokeColor="#D1D1D6"
            app:cornerRadius="12dp" />

    </LinearLayout>

</LinearLayout>
